class Solution {

    String modify(String s) {
        // your code here
        String str = "";
        for(int i = 0 ; i < s.length() ; i++){
            if(s.charAt(i) != ' '){
                str = str + s.charAt(i);
            }
        }
        return str;
    }
}
